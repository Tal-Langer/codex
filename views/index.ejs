<!DOCTYPE html>
<html>
<head>
  <title>Baby Prints</title>
</head>
<body>
  <%- include("partials/navbar") %>
  <h1>Products</h1>
  <ul>
    <% products.forEach(p => { %>
      <li>
        <a href="/product/<%= p.id %>"><%= p.title %></a> - $<%= p.price %>
      </li>
    <% }) %>
  </ul>
  <a href="/cart">View Cart</a>
  | <a href="/admin/login">Admin</a>
</body>
</html>
